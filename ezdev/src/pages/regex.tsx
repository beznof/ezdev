import React from 'react';
import Layout from '../layout/layout';

const Regex: React.FC = () => {
  return (
    <Layout>
      <div>
        <h1>Regex Page</h1>
      </div>
    </Layout>
  );
};

export default Regex;